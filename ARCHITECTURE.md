# Nirogya Healthcare Platform - Technical Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│                              NIROGYA HEALTHCARE PLATFORM                                         │
├─────────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                                  │
│   MODULE A: VIDEO CONSULTATION                                                                   │
│   React (WebRTC) → Node.js (Socket.IO) → Python Flask (Translation) → External APIs             │
│                                                                                                  │
│   MODULE B: VOICE AI ASSISTANT                                                                   │
│   Twilio (Telephony) → Ultravox (Voice AI) → LangChain (LLM Tools) → MongoDB (Data)             │
│                                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

# MODULE A: VIDEO CONSULTATION SYSTEM

## A1. Video Call Initialization

```
User Opens App
       │
       ▼
┌──────────────────────┐
│   LandingPage.jsx    │  User enters Room ID
└──────┬───────────────┘
       │ Navigate to /room/:roomId
       ▼
┌──────────────────────┐
│     Room.jsx         │  Main Video Call Component
│     MedicalRoom()    │
└──────┬───────────────┘
       │
       ├─────────────────────────────────────────┐
       │                                         │
       ▼                                         ▼
┌──────────────────────┐               ┌─────────────────────────┐
│ getUserMedia()       │               │ socket.connect()        │
│ Camera + Microphone  │               │ Connect to Node.js      │
└──────┬───────────────┘               └──────┬──────────────────┘
       │                                      │
       │ MediaStream                          │ WebSocket
       ▼                                      ▼
┌──────────────────────┐               ┌─────────────────────────┐
│ localVideoRef        │               │ socket.emit('join-room')│
│ Display local video  │               │ roomId                  │
└──────────────────────┘               └──────┬──────────────────┘
                                              │
                                              ▼
                                       ┌─────────────────────────┐
                                       │  Node.js Server         │
                                       │  server.js + socket.js  │
                                       └─────────────────────────┘
```

### Key Files
- `services/Client/src/Pages/LandingPage.jsx` - Room entry UI
- `services/Client/src/Pages/Room.jsx` - Main video call component
- `services/watch_connecting/server.mjs` - Express + Socket.IO server

---

## A2. WebRTC Peer Connection

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              WEBRTC SIGNALING FLOW                                   │
└─────────────────────────────────────────────────────────────────────────────────────┘

User A (First to Join = Doctor)              User B (Second to Join = Patient)
         │                                              │
         │ socket.emit('join-room')                     │
         ▼                                              │
┌─────────────────────┐                                 │
│  Node.js Server     │                                 │
│  rooms.set(roomId)  │◄────────────────────────────────┤ socket.emit('join-room')
│  room.add(socketId) │                                 │
└─────────┬───────────┘                                 │
          │                                             │
          │ emit('user-role', {role: 'doctor'})         │ emit('user-role', {role: 'patient'})
          │ emit('all-users', [])                       │ emit('all-users', [userA])
          ▼                                             ▼
┌─────────────────────┐                       ┌─────────────────────┐
│ setUserRole('doctor')│                      │ setUserRole('patient')│
│ fetchPatientData()  │                       │ createPeerConnection()│
└─────────────────────┘                       └─────────┬─────────────┘
                                                        │
                                                        │ RTCPeerConnection
                                                        ▼
                                              ┌─────────────────────┐
                                              │ pc.createOffer()    │
                                              │ pc.setLocalDescription│
                                              └─────────┬─────────────┘
                                                        │
                                                        │ socket.emit('signal', {type: 'offer'})
                                                        ▼
         ┌──────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────┐
│ pc.setRemoteDescription│
│ pc.createAnswer()   │
│ socket.emit('signal',│
│   {type: 'answer'}) │
└─────────┬───────────┘
          │
          │ ICE Candidates Exchange
          ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                         PEER-TO-PEER VIDEO/AUDIO STREAM                              │
│                                                                                      │
│  User A (Doctor) ◄══════════════════════════════════════════════► User B (Patient)  │
│                              WebRTC Media Stream                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

### ICE Server Configuration
```javascript
iceServers: [
  { urls: "stun:stun.l.google.com:19302" },
  { urls: "stun:stun1.l.google.com:19302" },
  { urls: "turn:openrelay.metered.ca:80", username: "openrelayproject", credential: "openrelayproject" },
  { urls: "turn:openrelay.metered.ca:443", username: "openrelayproject", credential: "openrelayproject" }
]
```

---

## A3. Real-Time Translation Pipeline

```
```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                        MULTILINGUAL TRANSLATION PIPELINE                             │
│                                                                                      │
│  Patient (Hindi) ──► STT ──► Translation ──► TTS ──► Doctor (English/Kannada)       │
│  Doctor (Kannada) ──► STT ──► Translation ──► TTS ──► Patient (Hindi)               │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           USER A (Patient - Hindi)                                   │
│  React App - Video + Mic + SimpleLanguageSelector                                   │
└───────────┬─────────────────────────────────────────────────────────┬───────────────┘
            │ WebRTC (Video/Audio)                                    │ Audio Recording
            │                                                         │ MediaRecorder API
            ▼                                                         ▼
┌───────────────────────────────────┐               ┌─────────────────────────────────┐
│  Node.js Backend                  │               │  useSimpleTranslation.js        │
│  Socket.IO + Express              │               │  Record audio chunks            │
│  Port: 8000                       │               │  Create audio blob              │
└───────────────────────────────────┘               └─────────────┬───────────────────┘
                                                                  │
                                                                  │ POST /translate
                                                                  │ FormData: audio.webm
                                                                  ▼
                                                    ┌─────────────────────────────────┐
                                                    │  Python Translation Service     │
                                                    │  simple_translation_server.py   │
                                                    │  Port: 5000                     │
                                                    └─────────────┬───────────────────┘
                                                                  │
                                    ┌─────────────────────────────┼─────────────────────────────┐
                                    │                             │                             │
                                    ▼                             ▼                             ▼
                          ┌─────────────────┐           ┌─────────────────┐           ┌─────────────────┐
                          │  WHISPER STT    │           │  GEMINI 2.0     │           │  gTTS           │
                          │  (OpenAI)       │           │  Translation    │           │  Text-to-Speech │
                          │  tiny/base model│           │  Flash Model    │           │  Google TTS     │
                          └────────┬────────┘           └────────┬────────┘           └────────┬────────┘
                                   │                             │                             │
                                   │ "Mujhe bukhar hai"          │ "I have fever"             │ audio.mp3
                                   │ (Hindi text)                │ (English text)             │
                                   └─────────────────────────────┴─────────────────────────────┘
                                                                  │
                                                                  │ Response: audio/mp3
                                                                  ▼
                                                    ┌─────────────────────────────────┐
                                                    │  Play translated audio          │
                                                    │  new Audio().play()             │
                                                    │  Update translationHistory      │
                                                    └─────────────────────────────────┘
                                                                  │
                                                                  ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           USER B (Doctor - English/Kannada)                          │
│  React App - Video + Translated Audio + Captions                                    │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Audio      │     │   Whisper    │     │   Gemini     │     │    gTTS      │
│   Input      │────►│   STT        │────►│   Translate  │────►│   TTS        │
│   .webm      │     │   Transcribe │     │   Text       │     │   .mp3       │
└──────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
     Hindi              "मुझे बुखार है"      "I have fever"       English Audio
```

### Supported Languages
| Code | Language | Code | Language |
|------|----------|------|----------|
| en | English 🇺🇸 | ta | Tamil 🇮🇳 |
| hi | Hindi 🇮🇳 | te | Telugu 🇮🇳 |
| kn | Kannada 🇮🇳 | bn | Bengali 🇮🇳 |
| mr | Marathi 🇮🇳 | gu | Gujarati 🇮🇳 |
| or | Odia 🇮🇳 | fr | French 🇫🇷 |
| es | Spanish 🇪🇸 | | |

### Key Files
- `services/Client/src/hooks/useSimpleTranslation.js` - Audio recording & API calls
- `services/Client/src/components/SimpleTranslationPanel.jsx` - Translation UI
- `services/translation-service/simple_translation_server.py` - Flask translation API

---

## A4. AI Voice Agent (AI-Only Mode)

```
User navigates to /room/onlyai/:roomId
              │
              ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│                              AI VOICE CONSULTATION MODE                               │
│                         Real-time Voice AI (like Gemini Live)                        │
└──────────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│ startAIVoiceConversation()│
└─────────┬───────────┘
          │
          ├─────────────────────────────────────────┐
          │                                         │
          ▼                                         ▼
┌─────────────────────┐               ┌─────────────────────────┐
│ Initialize Audio    │               │ Web Speech Recognition  │
│ AudioContext        │               │ SpeechRecognition API   │
│ Audio Element       │               │ continuous: true        │
└─────────────────────┘               └─────────┬───────────────┘
                                                │
                                                │ User speaks
                                                ▼
                                      ┌─────────────────────────┐
                                      │ recognition.onresult    │
                                      │ transcript = "I have    │
                                      │ headache and fever"     │
                                      └─────────┬───────────────┘
                                                │
                                                │ getAIVoiceResponse(transcript)
                                                ▼
                                      ┌─────────────────────────┐
                                      │ Gemini 2.0 Flash API    │
                                      │ "Dr. Nirogya" persona   │
                                      └─────────┬───────────────┘
                                                │
                                                │ AI Response
                                                ▼
                                      ┌─────────────────────────┐
                                      │ speakText()             │
                                      │ SpeechSynthesis API     │
                                      └─────────────────────────┘
```

---

## A5. Doctor Panel - Patient Data

```
Doctor joins room (isFirstUser = true)
              │
              ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              DOCTOR SIDE PANEL                                       │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                 │
│  │  Patient    │  │   Chat      │  │ Prescription│  │ AI Bot      │                 │
│  │  Info       │  │   Messages  │  │   Writer    │  │ Transcript  │                 │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘                 │
│                                                                                      │
│  Patient Info Tab:                 Prescription Tab:                                │
│  ├── Name, Age, Gender, Phone      ├── Medications input                            │
│  ├── Medical History               ├── Instructions input                           │
│  ├── AI Analysis (shortSummary)    ├── Follow-up date                               │
│  └── Vitals (if available)         └── Save/Print functionality                     │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

# MODULE B: VOICE AI ASSISTANT (Telephony)

## B1. Inbound Call Flow

```
User Dials Phone
       │
       ▼
┌──────────────┐
│    TWILIO    │  Receives PSTN call
└──────┬───────┘
       │ HTTP POST {From, CallSid}
       ▼
┌──────────────────────────┐
│  /api/twilio/inbound     │  routes/twilio.js
└──────┬───────────────────┘
       │
       ├──────────────────────────────────┐
       │                                  │
       ▼                                  ▼
┌──────────────────┐            ┌─────────────────────────────┐
│ getOrCreateUser()│            │ createUltravoxCallForTwilio()│
│ services/users.js│            │ services/twilio.js           │
└──────┬───────────┘            └──────┬──────────────────────┘
       │                               │
       │ findOne/create                │ POST /api/calls
       ▼                               │ {systemPrompt, tools, voice}
┌──────────────┐                       ▼
│   MongoDB    │               ┌───────────────┐
│   User       │               │  ULTRAVOX AI  │
└──────────────┘               │  API          │
                               └──────┬────────┘
                                      │ returns {joinUrl}
                                      ▼
                               ┌──────────────────┐
                               │ TwiML Response   │
                               │ <Connect><Stream>│
                               │ url={joinUrl}    │
                               └──────┬───────────┘
                                      │
                                      ▼
                               ┌──────────────┐
                               │   TWILIO     │
                               │ Streams Audio│◄────► User on Call
                               └──────┬───────┘
                                      │ WebSocket
                                      ▼
                               ┌───────────────┐
                               │  ULTRAVOX AI  │◄────► Voice Conversation
                               │  (Real-time)  │
                               └───────────────┘
```

### Key Files
- `server/src/routes/twilio.js` - `/api/twilio/inbound` endpoint
- `server/src/services/twilio.js` - `createUltravoxCallForTwilio()`
- `server/src/services/users.js` - `getOrCreateUser()`
- `server/src/lib/prompts.js` - `getInboundCallPrompt()`

---

## B2. Call End Webhook (Async Processing)

```
Call Ends
    │
    ▼
┌───────────────┐
│  ULTRAVOX AI  │  Fires webhook
└──────┬────────┘
       │ HTTP POST {event: "call.ended", call: {callId}}
       ▼
┌──────────────────────────┐
│  /api/ultravox/webhook   │  routes/ultravox.js
└──────┬───────────────────┘
       │
       ├─────────────────────────────────────────┐
       │                                         │
       ▼                                         ▼
┌─────────────────────┐                 ┌────────────────────────┐
│ getCallTranscript() │                 │ getCallDetails()       │
│ GET /api/calls/{id} │                 │ GET /api/calls/{id}    │
└──────┬──────────────┘                 └──────┬─────────────────┘
       │                                       │
       └───────────────┬───────────────────────┘
                       │
                       ▼
              ┌────────────────┐
              │ saveCallHistory│
              │ MongoDB        │
              └───────┬────────┘
                      │
                      ▼
         ┌────────────────────────────┐
         │ processTranscriptWithLLM() │
         │ services/langchain-agent.js│
         └────────────┬───────────────┘
                      │
                      │ Gemini 2.5 Flash
                      ▼
              ┌───────────────┐
              │ GOOGLE GEMINI │
              │ LLM           │
              └───────┬───────┘
                      │
                      │ Tool Calls
                      ▼
         ┌────────────────────────────┐
         │ Execute Tools:             │
         │ • createReminder() → DB    │
         │ • bookAppointment() → DB   │
         │ • updatePatientProfile()   │
         └────────────────────────────┘
```

### Key Files
- `server/src/routes/ultravox.js` - `/api/ultravox/webhook` endpoint
- `server/src/services/langchain-agent.js` - `processTranscriptWithLLM()`
- `server/src/services/call-history.js` - `saveCallHistory()`

---

# API REFERENCE

## Video Consultation APIs

### Node.js Server (Port 8000)
| Endpoint | Method | Description |
|----------|--------|-------------|
| `/` | GET | Server status |
| `/health` | GET | Health check with connection count |

### Socket.IO Events
| Event | Direction | Description |
|-------|-----------|-------------|
| `join-room` | Client → Server | Join a video room |
| `all-users` | Server → Client | List of existing users |
| `user-role` | Server → Client | Assigned role (doctor/patient) |
| `user-joined` | Server → Client | New user notification |
| `user-disconnected` | Server → Client | User left notification |
| `signal` | Bidirectional | WebRTC signaling (offer/answer/ICE) |

### Python Translation Service (Port 5000)
| Endpoint | Method | Description |
|----------|--------|-------------|
| `/` | GET | Service status |
| `/health` | GET | Health check (Whisper model status) |
| `/translate` | POST | Translate audio (FormData: audio, sourceLang, targetLang) |

---

## Voice AI APIs

### Server (Port 3001)
| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/twilio/inbound` | POST | Twilio webhook for inbound calls |
| `/api/twilio/outbound` | POST | Manual outbound call (testing) |
| `/api/ultravox/webhook` | POST | Ultravox call events |
| `/api/ultravox/tools/chat` | POST | LangChain agent proxy |
| `/api/ultravox/tools/book-appointment` | POST | Book appointment |
| `/api/ultravox/tools/get-available-slots` | POST | Get doctor slots |
| `/api/ultravox/tools/get-patient-profile` | POST | Get patient info |
| `/api/call-history` | GET | Get call history |
| `/health` | GET | Health check |

---

# TECHNOLOGY STACK

| Component | Stack |
|-----------|-------|
| **Frontend** | React + Vite, TailwindCSS, Lucide Icons, React Router |
| **Real-Time** | WebRTC (P2P video/audio), Socket.IO (signaling) |
| **Backend** | Node.js + Express, Python Flask (translation) |
| **AI/ML** | Whisper (STT), Gemini 2.0 (translation/LLM), gTTS (TTS), LangChain |
| **Voice** | Twilio (telephony), Ultravox (voice AI) |
| **Database** | MongoDB |
| **Fallback** | Web Speech API (browser STT/TTS) |

---

# EXTERNAL DEPENDENCIES

| Service | Purpose | Auth |
|---------|---------|------|
| **Twilio** | Telephony (calls, SMS) | `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN` |
| **Ultravox AI** | Voice AI conversations | `ULTRAVOX_API_KEY` |
| **Google Gemini** | LLM for translation & processing | `GOOGLE_API_KEY` |
| **MongoDB** | Data persistence | `MONGODB_URI` |
| **OpenAI Whisper** | Speech-to-text | Local model |
| **Patient API** | External patient records | `https://sih-2025-fc4t.onrender.com/api/tickets/:id` |

---

# SYSTEM SUMMARY

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                         IMPLEMENTED FEATURES (v1.0)                                  │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  MODULE A: VIDEO CONSULTATION                                                        │
│  ├── ✅ WebRTC P2P video/audio with Socket.IO signaling                             │
│  ├── ✅ Real-time translation (Hindi/English/Kannada + 8 more)                      │
│  ├── ✅ AI Voice Agent (Dr. Nirogya) for AI-only mode                               │
│  ├── ✅ Doctor Panel (patient info, chat, prescription, transcription)              │
│  └── ✅ Role System (auto-assign doctor/patient)                                    │
│                                                                                      │
│  MODULE B: VOICE AI ASSISTANT                                                        │
│  ├── ✅ Inbound calls via Twilio → Ultravox Voice AI                                │
│  ├── ✅ Call transcription and history storage                                      │
│  ├── ✅ LLM-powered post-call processing (reminders, appointments)                  │
│  └── ⚠️  Automated reminder scheduler (see FUTURE.md)                               │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

# Next Steps

See **[FUTURE.md](./FUTURE.md)** for planned enhancements:
- Automated reminder scheduler
- ElevenLabs integration for natural voice synthesis
- Vector database (Qdrant) for medical knowledge retrieval
- Enhanced doctor panel with AI-assisted medical history lookup
